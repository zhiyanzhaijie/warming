[package]
name = "ui"
version = "0.1.0"
edition = "2021"

[dependencies]
dioxus = { workspace = true }
api = { workspace = true }
rfd = { version = "0.17.2", optional = true }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1"

[features]
desktop = ["dep:rfd"]
web = []
